<!-- 
### SEO & Metadata Standards
- EVERY page component in the `app` directory MUST include a metadata definition.
- For STATIC pages (e.g., /about, /login), use the static `export const metadata: Metadata` object.
- For DYNAMIC pages (e.g., [id], [slug]), use the `export async function generateMetadata()` function.
- Always include `title`, `description`, and `openGraph` tags.
- When generating metadata for dynamic routes, always fetch the relevant record from Prisma to ensure the title reflects the real-world data (e.g., Subject Name, Student Name).
- Pattern: "Topic Name | Grade Level | SchoolPlatform"

### Multitenancy & Data Isolation
- EVERY database query using Prisma MUST include a `where` clause for `schoolId` unless the user is a `SUPER_ADMIN`.
- For B2C (Individual) users, ensure queries are filtered by `userId`.
- NEVER generate a delete or update function that doesn't verify the owner's `schoolId` or `userId`.

### Curriculum Terminology
- NEVER hard-code the words "Grade," "Term," or "Subject" in the UI.
- ALWAYS use the dynamic labels from the `Curriculum` model (`yearLabel`, `termLabel`, `subjectLabel`).
- Example: Use `{curriculum.yearLabel}` instead of "Grade".

### UX Standards: Loading & Errors
- EVERY form submission or button click that triggers a Server Action MUST use a `loading` state (e.g., disable button, show spinner).
- ALWAYS use `sonner` (toast) to show a success or error message after a database mutation.
- Use "Skeletons" (Shadcn Skeleton) for initial page loading states to improve perceived performance.

### AI Content Generation
- When generating AI lessons, the output must ALWAYS be a structured JSON object.
- JSON structure for Lessons: `{ title: string, content: string, quiz: { question: string, options: string[], answer: string }[] }`.
- This ensures the frontend can parse the AI data without crashing.

### Next.js Architecture
- Favor Server Components by default. 
- Only use `'use client'` at the top of a file if it handles user interaction (buttons, inputs) or browser-only APIs.
- Keep the component tree lean; move data fetching to the Page level (Server Component) 
-  -->



Act as a Senior Frontend Architect. Refactor the selected file(s) to convert hardcoded colors into our dynamic, multi-tenant design system.

### 1. CORE COLOR LOGIC (Theme Awareness)
Ensure base surfaces switch automatically between light and dark modes by replacing hardcoded slate/white with these tokens:
- Replace `bg-slate-950` or `bg-white` with `bg-background` (Handles the main page canvas).
- Replace `bg-slate-900` or `bg-gray-50` with `bg-card` (Handles the "lifted" card surfaces).
- Replace `border-white/5` or `border-slate-200` with `border-border`.
- Replace `text-white` (on dark) or `text-slate-950` (on light) with `text-foreground`.
- Replace `text-slate-400/50` with `text-muted-foreground`.

### 2. INSTITUTIONAL BRANDING (Dynamic Tokens)
Every school has its own brand colors stored in the database. Use our custom Tailwind aliases instead of hardcoded hex codes:
- Use `bg-school-primary` for all primary actions, progress bars, and active states.
- Use `bg-school-secondary` for structural elements like sidebars or secondary surfaces.
- IMPORTANT: For text sitting on a brand-colored background, use the utility class `text-on-school-primary` (for primary) or `text-on-school-secondary` (for secondary) to ensure WCAG accessibility.
- Use Tailwind opacity for subtle accents: e.g., `bg-school-primary/10` for icon backgrounds or `border-school-primary/20` for soft borders.

### 3. "BRUTAL-ELEGANT" TYPOGRAPHY & RADIUS
Maintain the high-stakes institutional registry look:
- Main Section Headers: Must be `font-black uppercase italic tracking-tighter`.
- Labels, Badges, and Metadata: Must be `text-[10px] font-black uppercase tracking-widest`.
- Card Corners: Use large radii, specifically `rounded-[2.5rem]` or `rounded-[3rem]`.

### 4. ARCHITECTURAL CONSTRAINTS
- NO "ANY": Strictly type every variable. If casting from Prisma JSON, use `(data as unknown as SpecificInterface)`.
- NO PROP DRILLING: If you need the User Identity, SchoolId, or PrimaryColor, import `useProfileStore` from `@/store/profileStore` instead of passing props from the layout.
- SERVER-FIRST: If this is a `page.tsx`, it must be an `async` Server Component. Fetch data using our Server Actions and pass them as props to a Client Component (`"use client"`).
- DYNAMIC SEO: Every `page.tsx` must export a `generateMetadata` function that includes the school name or specific module title in the page title.

### 5. EXECUTION
- Do not remove any existing business logic or database calls.
- Ensure all Lucide icons are appropriately scaled and themed with the school-primary color where they represent status.